*** Begin Patch
*** Update File: simple_head_drive.py
@@
-            if self.mouth_accel_max > self.mouth_accel_start:
-                accel_intensity = (mouth_ratio - self.mouth_accel_start) / (
-                    self.mouth_accel_max - self.mouth_accel_start
-                )
-            else:
-                accel_intensity = 0.0
-            accel_intensity = max(0.0, min(1.0, accel_intensity))
-
-            tongue_active = (
-                self.tongue_brake_threshold is not None
-                and tongue_ratio <= self.tongue_brake_threshold
-                and mouth_ratio >= self.mouth_accel_start * 0.6
-            )
-
-            if dy > self.reverse_threshold:
-                motion = "reverse"
-            elif tongue_active:
-                motion = "brake"
-            elif accel_intensity > 0:
-                motion = "accelerate"
-            else:
-                motion = "idle"
+            if self.mouth_accel_max > self.mouth_accel_start:
+                accel_intensity = (mouth_ratio - self.mouth_accel_start) / (
+                    self.mouth_accel_max - self.mouth_accel_start
+                )
+            else:
+                accel_intensity = 0.0
+            accel_intensity = max(0.0, min(1.0, accel_intensity))
+
+            eyebrow_raised = (
+                self.eyebrow_baseline is not None
+                and self.eyebrow_raise_threshold is not None
+                and (self.eyebrow_baseline - metrics['eyebrow_height']) >= self.eyebrow_raise_threshold
+            )
+
+            if dy > self.reverse_threshold:
+                motion = "reverse"
+            elif eyebrow_raised:
+                motion = "brake"
+            elif accel_intensity > 0:
+                motion = "accelerate"
+            else:
+                motion = "idle"
@@
-            elif motion == "brake":
-                motion_label = "BRAKE (tongue)"
+            elif motion == "brake":
+                motion_label = "BRAKE (eyebrows)"
*** End Patch
